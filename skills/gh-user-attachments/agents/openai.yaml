interface:
  display_name: "GitHub User Attachments"
  short_description: "Upload 2–10 files to GitHub attachment URLs"
  default_prompt: "Use $gh-user-attachments to upload these 2–10 local files and return finalized GitHub attachment URLs in input order."
